import styled from "styled-components";

export const Container = styled.h1`
  grid-area: UH;

  display: flex;

  padding: 0 30px;

  background: #8b83f0;
  color: var(--white);

  align-items: center;
  justify-content: space-between;

  h1 {
    font-size: 1em;
  }

  div {
    display: flex;
    align-items: center;
  }
`;
